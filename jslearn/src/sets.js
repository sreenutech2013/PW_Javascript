
let arr = ['a', 'b', 'a']

console.log(arr)

let setOfElements = new Set(['a', 'b', 'a'])

console.log(setOfElements)

let setList = new Set();

let val = 'first'
setList.add(val)
setList.add('second')
setList.add('thrid')

console.log(setList)

// for (let x of setList.values()) {
//     console.log(x)
// }

setList.size

//eg :- testdata within csv
// name,age,desg
//  kumar,40,qe
//  suresh.41,qe


let csvTestdata = "filename"

csvTestdata.forEach(function (data) {
    // 100 line test cases
    
    data.name


})
setList.forEach(function(value) {
console.log(value)
test()
})

//set --> add(), delete(), foreach, values(), has('first'), size


